[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "neural-ai-next"
version = "1.0.0"
description = "Hierarchical trading system with machine learning"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [
    {name = "Elynea-1528", email = "szabogergely152800@gmail.com"}
]
keywords = ["trading", "machine-learning", "neural-networks", "metatrader", "forex"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Investment",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    # Core web framework
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "websockets>=12.0",
    
    # Data validation & serialization
    "pydantic>=2.4.0",
    "pydantic-settings>=2.1.0",
    
    # Database
    "sqlalchemy[asyncio]>=2.0.0",
    "aiosqlite>=0.19.0",
    "alembic>=1.12.0",
    
    # Logging
    "structlog>=23.1.0",
    
    # CLI
    "typer>=0.9.0",
    
    # HTTP client
    "requests>=2.31.0",
    "aiohttp>=3.9.0",           # JForex Collector async letöltéshez

    # Event bus (ZeroMQ)
    "pyzmq>=25.0",              # EventBus (core/events) és JForex live feed (collectors/jforex)
    
    # YAML config
    "pyyaml>=6.0",
    
    # Multipart form data
    "python-multipart>=0.0.6",
    
    # Data processing
    "polars>=0.20.0",
    
    # Utilities
    "packaging>=23.1",
    "tenacity>=8.2.0",          # JForex Collector retry mechanizmushoz
    "psutil>=5.9.0",            # HealthMonitor rendszermetrikák (core/system)
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-xdist>=3.5.0",  # Parallel execution for test isolation
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.21.0",
    "pytest-json-report>=1.5.0",  # FIX 2: Pass/Fail/Err/Skip oszlopok
    "pytest-mock>=3.15.0",  # Mock support
    "mypy>=1.5.0",
    "bandit>=1.7.7",
    "ruff>=0.1.0",  # RUFF IS KING
    "pre-commit>=3.4.0",
    "asyncpg>=0.29.0",  # PostgreSQL async driver (16 DB teszt)
    "types-pyyaml>=6.0.0",
    "types-requests>=2.31.0",
    "types-python-dateutil>=2.8.0",
    "types-setuptools>=69.0.0",
    "types-pytz>=2023.3.0",
    "types-six>=1.16.0",
    "pandas-stubs>=2.0.0",
]

trader = [
    "ib_insync>=0.9.70",
    "vectorbt>=0.25.0",
    "dukascopy-python",
    "python-socketio>=5.0.0",
    "redis>=4.0.0",
]

jupyter = [
    "jupyterlab>=4.0.0",
    "notebook>=7.0.0",
    "ipykernel>=6.0.0",
    "matplotlib>=3.5.0",
    "seaborn>=0.12.0",
    "plotly>=5.0.0",
    "kaggle>=1.5.0",
    "tensorboard>=2.14.0",
]

ui = [
    "streamlit>=1.30.0",
    "plotly>=5.18.0",
    "streamlit-aggrid",
    "watchdog",
    "tensorboard",
    "torchinfo",
]

full = [
    "neural-ai-next[dev]",
    "neural-ai-next[trader]",
    "neural-ai-next[jupyter]",
    "neural-ai-next[ui]",
]

[project.urls]
Homepage = "https://github.com/Elynea-1528/neural-ai-next"
Repository = "https://github.com/Elynea-1528/neural-ai-next"
Issues = "https://github.com/Elynea-1528/neural-ai-next/issues"

[tool.hatch.build.targets.wheel]
packages = ["neural_ai"]

# --- RUFF CONFIGURATION (Replaces Black, Isort, Flake8) ---
[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "D",   # pydocstyle (Docstrings)
]
ignore = [
    "D403", # First word of the first line should be properly capitalized (EZT KAPCSOLTUK KI!)
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

# --- MYPY CONFIGURATION ---
[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
strict_equality = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

# --- PYTEST CONFIGURATION ---
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
# KRITIKUS FIX: -n auto ELTÁVOLÍTVA (coverage incompatibilis)
# Ha gyors tesztelés kell: pytest -n auto
# Ha coverage kell: coverage run -m pytest (szekvenciális)
addopts = "-v --tb=short --strict-markers"
asyncio_mode = "auto"
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "asyncio: AsyncIO tests",
]

[tool.coverage.run]
source = ["neural_ai"]
omit = ["*/tests/*"]