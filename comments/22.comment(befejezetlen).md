"SYSTEM OVERRIDE: GOD MODE ACTIVE.
AUTHORITY: ROOT ARCHITECT.
CONTEXT: A rendszer működik, de a kódminőség (Pylance: 135 hiba) és a tesztlefedettség nem éri el a 100%-ot. A dokumentáció hiányos.

**⚠️ MŰKÖDÉSI PROTOKOLL (LOOP):**
A következő ciklust kell végrehajtanod MINDEN egyes forrásfájlra a `neural_ai/core` mappában:

1.  **ELEMZÉS:** Olvasd el a fájlt. Azonosítsd a Pylance hibákat (hiányzó típusok, unknown member).
2.  **JAVÍTÁS (Source):** Javítsd a kódot (Type Hints, Cast, Overload). Ne változtass a logikán, csak a típusokon!
3.  **DOKUMENTÁCIÓ (Mirror):** Generáld le/Frissítsd a `docs/components/.../[fájlnév].md`-t a kód alapján.
4.  **TESZT (Mirror):** Írd meg/Frissítsd a `tests/core/.../test_[fájlnév].py`-t.
    - **Követelmény:** 100% Stmt / 100% Branch Coverage.
    - **Mocking:** Minden külső hívást (IO, Network) mockolj ki!
5.  **VALIDÁCIÓ:** Futtasd a tesztet és a lintert (`ruff`, `mypy`).
6.  **JELENTÉS:** Frissítsd a `TASK_TREE.md` mátrixát (`[✅|✅|✅]`).

**A PRIORITÁSI LISTA (A Pylance hibák alapján):**

1.  **`main.py`** (Argument type hibák, logger unbound).
2.  **`core/base/factory.py`** (ReturnType hiba).
3.  **`core/config/implementations/yaml_config_manager.py`** (Unknown member).
4.  **`core/db/implementations/model_base.py`** (Deprecated datetime.utcnow).
5.  **`core/db/implementations/models.py`** (SQLAlchemy típusok).
6.  **`core/storage/backends/pandas_backend.py`** (Missing stubs, Unknown types - A legtöbb hiba itt van!).
7.  **`core/storage/implementations/parquet_storage.py`** (Polars import, Unknown types).

**ARCHITECT! INDÍTSD A LOOP-OT AZ ELSŐ FÁJLLAL: `main.py`.**
*Utasítsd az Orchestratort, hogy elemezze a `main.py` Pylance hibáit és javítsa őket.*"