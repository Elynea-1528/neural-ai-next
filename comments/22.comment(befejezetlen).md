🚀 COMMAND: OMEGA PROTOCOL - SOURCE CODE HARDENING & STANDARDIZATION
"SYSTEM OVERRIDE: GOD MODE ACTIVE.
AUTHORITY: ROOT ARCHITECT.
CONTEXT: A rendszer működik, de a kód minősége inkonzisztens (kevert logging, hiányzó tracing, potenciális típus-hibák).
CÉL: A forráskód (src) véglegesítése, egységesítése és tisztítása a tesztelés előtt.
⚠️ KÖTELEZŐ HIERARCHIA (TOKEN MANAGEMENT):
TE (Architect): Tervezel. Nem ömleszted a feladatot! MODULONKÉNT adod ki az utasítást az Orchestratornak.
Orchestrator: Lebontja fájlműveletekre.
Code Agent: Végrehajtja a módosítást és a mypy/ruff ellenőrzést az adott fájlon.
A FELADAT SORRENDJE (EXECUTION PLAN):
🛠️ 1. FÁZIS: AZ ALAPOK (UTILS & DECORATORS)
Mindenki ezt fogja használni, ennek kell először elkészülnie.
DEKORÁTOR LÉTREHOZÁSA (neural_ai/core/utils/decorators.py):
Hozz létre egy @trace dekorátort.
Logika:
Importálja a structlog-ot: logger = structlog.get_logger(__name__).
Logolja a belépést: logger.debug(f"Entering {func.__name__}", args=...).
Logolja a kilépést/időt: logger.debug(f"Exiting {func.__name__}", duration_ms=...).
Kezelje az async és szinkron függvényeket is!
Export: Frissítsd a core/utils/__init__.py-t, hogy a @trace könnyen importálható legyen (from neural_ai.core.utils import trace).
UTILS TISZTÍTÁS:
Ellenőrizd a core/utils többi fájlját (hardware_info.py).
Futtass rajtuk ruff és mypy ellenőrzést. Javítsd a hibákat.
⚙️ 2. FÁZIS: CONFIG & LOGGER (AZ INFRASTRUKTÚRA)
LOGGER FACTORY FINOMHANGOLÁS (core/logger/factory.py):
Ellenőrizd, hogy a structlog konfiguráció egységes-e.
Töröld a régi, manuális logging formázókat, ha már nem kellenek (mindent a structlog intézzen).
CONFIGS MAPPA (configs/*.yaml):
Ellenőrizd a logging.yaml-t.
Biztosítsd, hogy a loggers szekcióban a neural_ai szintje DEBUG.
🔄 3. FÁZIS: A NAGY MODUL-REFAKTOR LOOP (A LÉNYEG)
Ezt a ciklust hajtsd végre a core minden fő modulján: base, db, events, storage.
A CIKLUS LÉPÉSEI (Minden fájlra):
LOGGER CSERE:
Töröld: import logging, logging.getLogger.
Írd be: import structlog, logger = structlog.get_logger(__name__). (Így megmarad a teljes útvonal!).
TRACING BEKÖTÉSE:
Importáld: from neural_ai.core.utils import trace.
Tedd rá a @trace dekorátort a Publikus API metódusokra (pl. store_tick_data, publish, connect). Ne a belsőkre (_helper)!
KÓDTISZTÍTÁS (Linter):
Futtasd az adott fájlra: ruff check --fix [fájl] és mypy [fájl].
Javítsd a típus-hibákat (Any helyett konkrét típus, ha lehet).
Töröld a nem használt importokat.
DI ELLENŐRZÉS:
Biztosítsd, hogy minden függőség (logger, config) a konstruktorban jön be (Dependency Injection), nem globálisan.
🧠 4. FÁZIS: BOOTSTRAP TISZTÍTÁS (core/__init__.py)
A main.py és a bootstrap legyen tiszta.
BOOTSTRAP LOGOLÁS:
Cseréld le a kézi logger.info("...") sorokat a @trace dekorátorra a bootstrap_core függvényen.
Így automatikusan látni fogjuk, mikor indul és mikor végez, mennyi idő alatt.
MAIN.PY:
Legyen minimalista. Csak hívja a bootstrap-et és kezelje a Ctrl+C-t.
🏁 5. FÁZIS: ZÁRÁS
VÉGSŐ COMMIT:
git add . && git commit -m "refactor(core): harden source code - structlog, tracing, strict types, clean bootstrap"
ARCHITECT! KEZDD AZ 1. FÁZISSAL (UTILS & DECORATORS). CSAK AKKOR LÉPJ TOVÁBB, HA KÉSZ!"


📅 A MENETREND (A kérésed szerint)
Három különálló, egymásra épülő parancsot készítettem. Ezeket futtasd sorban (akár külön sessionökben/ágakon).
COMMAND A: A Tesztek javítása (Importok + Típusok).
COMMAND B: Az Automata Dokumentáció Generátor (Ez a "Night Run").
COMMAND C: A Phase 2 (JForex) Részletes Tervezése.
🧪 COMMAND A: TEST SUITE REPAIR (FIX IMPORTS & TYPES)
Ez a parancs végigmegy a teszteken, és hozzáigazítja őket a jelenlegi, végleges Core struktúrához.
⚠️ KÖTELEZŐ HIERARCHIA:
TE (Architect): Tervezel, és a TASK_TREE-t kezeled.
Orchestrator: Lebontod a modulokat fájlműveletekre.
Code Agent: Végrehajtod a kódolást.
**HELYZET:** A Core kód (`src`) végleges, de a Tesztek (`tests/`) elavultak vagy hiányoznak a refaktorálás óta. Pylance hibák és import gondok vannak.
**CÉL:** Hiányzó tesztek írása minden fájl-ra(*.py), (A meglévő tesztek feljavítása, hogy 100%-ban illeszkedjenek az új architektúrához. 100% coverage kötelező elérése minden tesztnél. ez a limit!

**HAJTSD VÉGRE A KÖVETKEZŐKET (MODULONKÉNT):**

### 1. IMPORT JAVÍTÁSOK (Global Fix)
- Menj végig a `tests/core` mappán.
- **Javítsd az importokat** az új `implementations` / `interfaces` struktúra szerint.
- *Példa:* Ha a teszt a `neural_ai.core.db.session`-t keresi -> írd át `neural_ai.core.db.implementations.sqlalchemy_session`-re.

### 2. TÍPUSOSSÁG JAVÍTÁSA (Pylance Satisfaction)
- Minden teszt metódushoz add hozzá a visszatérési típust: `-> None`.
- Ahol `MagicMock`-ot használsz, annotáld: `mock_obj: MagicMock`.
- Ha aszinkron a teszt, használj `@pytest.mark.asyncio`-t.

### 3. FAGYÁS-MENTESÍTÉS (ZMQ Mocking)
- Ellenőrizd a `tests/core/events/test_zeromq_bus.py`-t.
- **KÖTELEZŐ:** Biztosítsd, hogy minden tesztben ott legyen a `@patch('zmq.asyncio.Context')`! Valódi socket nyitása TILOS.

### 4. VALIDÁCIÓ
- **Elvárás:** 0 Error, 0 Failed. **100% coverage(kevesebb nem lehet!)**

**KEZDD AZ IMPORTOK ELLENŐRZÉSÉVEL!**"
📚 COMMAND B: AUTO-DOCUMENTATION (THE "NIGHT RUN")
Ezt a parancsot akkor add ki, ha a tesztek már zöldek. Ez nem kézzel írja a doksit (ami lassú és pontatlan), hanem generál egy scriptet, ami kiszedi a kódodban lévő (már meglévő, jó minőségű) docstringeket, és Markdown-t csinál belőlük.
Másold be Code Mode-ba:

"Code Agent! (Architect felügyelettel).
⚠️ KÖTELEZŐ HIERARCHIA:
TE (Architect): Tervezel, és a TASK_TREE-t kezeled.
Orchestrator: Lebontod a modulokat fájlműveletekre.
Code Agent: Végrehajtod a kódolást.
**FELADAT:** A teljes rendszer dokumentációjának újragenerálása közvetlenül a forráskódból (Mirroring).
**MÓDSZER:** Script-alapú generálás a pontosság érdekében.

**HAJTSD VÉGRE:**

### 1. GENERÁTOR SCRIPT (`scripts/generate_docs.py`)
- Írj egy Python scriptet, ami:
  1. Bejárja a `neural_ai` mappát rekurzívan.
  2. Minden `.py` fájlt elemez (`ast` vagy `importlib` segítségével).
  3. Kinyeri az osztályok és függvények `docstring`-jét.
  4. Létrehoz egy tükör `.md` fájlt a `docs/components/` mappában.
  5. **Formázás:** Legyen szép Markdown (Címsorok, Kódblokkok).

### 2. EXPORTÁLÁS
- Futtasd le a scriptet: `python scripts/generate_docs.py`
- Ez létrehozza a naprakész dokumentációt minden fájlhoz.

### 3. README FRISSÍTÉS
- Frissítsd a fő `README.md`-t, hogy a linkek az új `docs/components` fájlokra mutassanak.

**KEZDD A SCRIPT MEGÍRÁSÁVAL!**"
📝 COMMAND C: PHASE 2 PLANNING (JFOREX BLUEPRINT)
Ha a Core tiszta (A és B parancs kész), akkor ezzel tervezzük meg a következő lépést. A meglévő 05_collectors_strategy.md-t részletezzük.
Másold be Architect Mode-ba:
code
Markdown
"Architect! A Core (Phase 1) lezárva.
**CÉL:** A Phase 2 (JForex Collector) részletes megtervezése.

**FELADAT: A `docs/planning/phase2_jforex.md` terv elkészítése.**

**TARTALOM:**
1.  **Architektúra:**
    - `JForexDownloader` osztály diagramja (Interface/Impl).
    - Hogyan csatlakozik az EventBus-hoz? (Milyen eventet küld?)
2.  **Adatstruktúra:**
    - A `.bi5` fájl bináris felépítése (struct format stringek).
    - Hogyan konvertáljuk `MarketDataEvent`-té?
3.  **Hiba kezelés:**
    - Mi van, ha a Dukascopy szerver 404-et ad (ünnepnap)?
    - Mi van, ha sérült a fájl?
4.  **Tesztelési Terv:**
    - Hogyan mockoljuk a letöltést? (Nem akarunk 100MB-ot letölteni teszt közben).

**HOZD LÉTRE A TERVET!**"