🚀 COMMAND: PHOENIX REBOOT - CONFIGURATION & CLEANUP
"Code Agent! (Architect felügyelettel).
MÓDVÁLTÁS: KONFIGURÁCIÓS INFRASTRUKTÚRA ÉPÍTÉS.
HELYZET: A kódváz kész, de a "szív" (Config) hiányzik. A dokumentáció túlkomplikált.
HAJTSD VÉGRE A KÖVETKEZŐKET (SORRENDBEN):
1. DOKUMENTÁCIÓ RESET (Tiszta Lap)
A) TASK TREE EGYSZERŰSÍTÉS:
Írd felül a docs/development/TASK_TREE.md-t.
Tartalom: Csak egy egyszerű Markdown Checklist legyen (semmi mátrix/progress bar):
code
Markdown
# 🌳 NEURAL AI NEXT - FELADATLISTA

## 🟢 KÉSZ (DONE)
- [x] Core Architektúra (Interface/Impl/Factory)
- [x] Dependency Injection (Container)
- [x] Base Components (Logger, Storage, DB, Events váz)

## 🟡 FOLYAMATBAN (CURRENT FOCUS)
- [ ] **Configuration Structure** (configs/ mappa)
- [ ] **Environment Setup** (.env fájl)
- [ ] **Bootstrap Wiring** (A configok bekötése a kódba)

## 🔴 HÁTRALÉVŐ (TODO)
- [ ] Database Dynamic Config Implementáció
- [ ] Collectors (JForex, MT5)
- [ ] Strategy Engine
- [ ] Dashboard (HTML/Live)
B) ÖTLETEK KONSZOLIDÁLÁSA (docs/ideas.md):
Írd felül a fájlt ezzel a listával:
Dead Letter Queue: Hibás Eventek mentése DB-be vagy fájlba.
Health Check: main.py percenként kérdezze le a modulok státuszát.
Log Rotation: 10MB-os fájlok, max 10 backup (Configban beállítva).
Schema Evolution: Parquet séma változás kezelése.
Data Gap Detection: Lyukas adatok automatikus detektálása.
Live Dashboard: FastAPI alapú webes felület a logokhoz és státuszhoz.
Remote Logging: Logok küldése központi szerverre.
MongoDB Support: Lehetőség NoSQL-re váltani a jövőben.
2. CONFIG STRUKTÚRA LÉTREHOZÁSA (A "HÚS")
Hozd létre a configs/ mappát a gyökérben, és töltsd fel ezekkel a fájlokkal:
configs/system.yaml:
code
Yaml
app_name: "Neural AI Next"
version: "0.5.0"
environment: "development" # production, staging
debug: true
paths:
  data: "data"
  logs: "logs"
  models: "models"
  cache: ".cache"
configs/logging.yaml (A User régi, profi konfigja alapján!):
code
Yaml
default_level: "INFO"
format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
date_format: "%Y-%m-%d %H:%M:%S"
handlers:
  console:
    enabled: true
    level: "INFO"
    colored: true
  file:
    enabled: true
    level: "DEBUG"
    filename: "logs/neural_ai.log"
    rotating: true
    max_bytes: 10485760
    backup_count: 10
    json_format: true
loggers:
  neural_ai.core: { level: "DEBUG" }
  neural_ai.events: { level: "INFO" }
  neural_ai.db: { level: "WARNING" }
  neural_ai.storage: { level: "INFO" }
configs/events.yaml:
code
Yaml
type: "zeromq"
connection:
  protocol: "tcp"
  host: "127.0.0.1"
  pub_port: 5555
  sub_port: 5556
  use_inproc: false # Teszteléshez true
configs/database.yaml:
code
Yaml
type: "sqlite" # sqlite, postgresql
connection:
  database: "neural_ai.db"
  # host: "localhost"
  # port: 5432
  # user: "${DB_USER}" # Env változó lesz
  # password: "${DB_PASSWORD}"
pool:
  size: 5
  timeout: 30
configs/storage.yaml:
code
Yaml
type: "parquet" # file, parquet, s3
base_path: "data/tick"
compression: "snappy" # snappy, gzip, brotli
engine: "auto" # auto (polars/pandas), fastparquet, pyarrow
partitioning:
  - "year"
  - "month"
  - "day"
3. ENVIRONMENT FILE (.env)
Hozd létre a .env és .env.example fájlokat a gyökérben:
code
Bash
APP_ENV=development
LOG_LEVEL=INFO
# Secrets
DB_USER=admin
DB_PASSWORD=secret
JFOREX_TOKEN=xyz
4. LEZÁRÁS
git add .
git commit -m "feat(config): scaffold configuration architecture (yaml files + env + task reset)"
INDÍTSD A TASK TREE TAKARÍTÁSSAL!"