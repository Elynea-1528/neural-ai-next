"SYSTEM OVERRIDE: GOD MODE ACTIVE.
AUTHORITY: ROOT ARCHITECT.
CONTEXT: A forráskód (`src`) kész és stabil, de a tesztek (`tests`) elavultak, inkompatibilisek és hiányosak. A User bizalma 0-n áll.
**PARANCS:** A teljes tesztkészlet szinkronizálása a kóddal, fájlról fájlra. Nincs hibatűrés.

**⚠️ VÉGREHAJTÁSI PROTOKOLL (LOOP):**
TE (Architect): Tervezel. Nem ömleszted a feladatot! MODULONKÉNT adod ki az utasítást az Orchestratornak.
Orchestrator: Lebontja fájlműveletekre.
A Code Agentnek az alábbi ciklust kell végrehajtania **MINDEN** egyes fájlon a `neural_ai` mappában (beleértve az `__init__.py`-kat és `exceptions`-öket is!):

1.  **READ:** Olvasd el a forráskódot (pl. `factory.py`, `jforex_error.py`, `__init__.py`).
2.  **ANALYZE:** Nézd meg a definíciókat (osztályok, függvények, konstansok, exportok).
3.  **TEST CHECK:** Nyisd meg a hozzá tartozó tesztet.
    - Ha hiányzik -> **HOZD LÉTRE.**
    - Ha régi importot használ -> **JAVÍTSD.**
    - Ha hiányzik paraméter a konstruktorból (pl. `storage`) -> **PÓTOLD.**
    - Ha `await`-et hívsz `MagicMock`-on -> **CSERÉLD `AsyncMock`**-ra!
    - Ha hálózatot használ (ZMQ) -> **MOCKOLD KI (`patch`)!**
    - **Init fájloknál:** Ellenőrizd, hogy az exportált modulok (`__all__`) elérhetőek-e.
    - **Exception fájloknál:** Ellenőrizd, hogy a hiba példányosítható-e és helyes-e az üzenete.
4.  **RUN:** Futtasd le CSAK azt az egy tesztfájlt.
5.  **REPEAT:** Ha nem 100% vagy hiba van, javítsd újra.

---

### 📋 1. LÉPÉS: TASK TREE EGYSZERŰSÍTÉS (NO BULLSHIT)
- Nyisd meg a `docs/templates/task_tree_template.md`-t és a `docs/development/TASK_TREE.md`-t.
- **TÖRÖLD KI** a grafikus progress barokat (`[🟩🟩...]`).
- **HELYETTE:** Csak egyszerű százalék legyen (pl. `Status: 🟢 DONE (95%)`).
- Legyen átlátható, tiszta lista.

---

### 🧪 2. LÉPÉS: A TELJES LISTA (MINDEN FÁJLRA)

**A) GYÖKÉR ÉS INIT (A Belépési Pontok)**
- `neural_ai/__init__.py`: Teszteld a verzió és sémaverzió exportálást.
- `neural_ai/core/__init__.py` (Bootstrap):
  - **KRITIKUS:** Teszteld a `bootstrap_core`-t. Ellenőrizd, hogy TÉNYLEG mindent létrehoz-e (DB, Bus, Storage, Persister).
  - Mockold a `ConfigFactory`-t, hogy ne fájlból olvasson.

**B) BASE MODUL (`core/base`)**
- `interfaces/*.py`: Ellenőrizd, hogy absztraktak-e.
- `implementations/singleton.py`: Teszteld a `metaclass` működését (egyedi példány?).
- `implementations/di_container.py`: Teszteld a regisztrációt és a feloldást.
- `implementations/component_bundle.py`: **FONTOS!** Ellenőrizd az összes mezőt (`persister`, `live_feed`, `database`, `hardware`...)!
- `implementations/lazy_loader.py`: Teszteld, hogy tényleg csak híváskor tölt-e be.
- `exceptions/*.py`: Teszteld a kivételek öröklődését.
- `factory.py`: Ellenőrizd a `CoreComponentFactory` összes metódusát.

**C) CONFIG MODUL (`core/config`)**
- `interfaces/*.py`
- `implementations/yaml_config_manager.py`: Teszteld a `load_directory`-t (namespace logika).
- `implementations/dynamic_config_manager.py`: Teszteld a DB fallback logikát.
- `exceptions/*.py`
- `factory.py`

**D) LOGGER MODUL (`core/logger`)**
- `interfaces/*.py`
- `implementations/*.py` (Colored, Default, Rotating): Teszteld a formázást és a fájlba írást (temp könyvtárral).
- `formatters/*.py`: Teszteld a színeket.
- `exceptions/*.py`
- `factory.py`: Teszteld a `configure` metódust (structlog vs standard logging).

**E) UTILS MODUL (`core/utils`)**
- `interfaces/*.py`
- `implementations/hardware_info.py`: Teszteld a `/proc/cpuinfo` olvasást (mockolt fájllal).
- `decorators.py`: Teszteld a `@trace` dekorátort (logol-e?).
- `exceptions/*.py`
- `factory.py`

**F) DATABASE MODUL (`core/db`)**
- `interfaces/*.py`
- `implementations/model_base.py`: Teszteld a `__tablename__` generálást.
- `implementations/models.py`: Teszteld a modellek (DynamicConfig, LogEntry) példányosítását.
- `implementations/sqlalchemy_session.py`:
  - **KÖTELEZŐ:** Használj `sqlite+aiosqlite:///:memory:`-t a teszthez.
  - Teszteld a `get_session` context managert.
- `exceptions/*.py`
- `factory.py`

**G) EVENTS MODUL (`core/events`)**
- `interfaces/event_models.py`: Teszteld a Pydantic validációt (`source`, `direction`).
- `interfaces/event_bus_interface.py`
- `implementations/zeromq_bus.py`:
  - **KÖTELEZŐ MOCK:** `@patch('zmq.asyncio.Context')`.
  - Teszteld a batch publish-t (lista kezelése).
- `exceptions/*.py`
- `factory.py`: Ellenőrizd a `create_from_config` metódust.

**H) STORAGE MODUL (`core/storage`)**
- `interfaces/*.py`
- `implementations/file_storage.py`: Teszteld a `write/read` metódusokat `**kwargs` támogatással.
- `implementations/parquet_storage.py`:
  - **KÖTELEZŐ:** Mockold a `HardwareInterface`-t a konstruktorban!
  - Teszteld a `_get_path` egyedi fájlnév generálását.
- `services/market_data_persister.py`:
  - **KRITIKUS:** Teszteld a `list` (batch) fogadását az `on_market_data`-ban!
- `services/resampler_service/*`: Teszteld a resampling logikát (mockolt DataFrame-mel).
- `backends/*.py`: Teszteld a Polars/Pandas wrapper-eket.
- `exceptions/*.py`
- `factory.py`

**I) COLLECTORS MODUL (`collectors/jforex`)**
- `interfaces/*.py`
- `implementations/bi5_downloader.py`:
  - **KÖTELEZŐ:** Add át a `storage` mockot a konstruktornak.
  - Teszteld a 0-bájtos fájl kezelését.
- `implementations/live_feed.py`:
  - Teszteld a `MarketDataEvent` létrehozását (helyes `source` mező!).
- `exceptions/*.py`
- `factory.py`: Ellenőrizd, hogy a `create_downloader` átadja-e a storage-ot.

---

### 🏁 3. LÉPÉS: VÉGSŐ VALIDÁCIÓ
- Ha minden modul kész, futtass egy **TELJES** tesztet:
  `pytest`
- **ELVÁRÁS:** 0 Failed, 0 Error.
- **COMMIT:** `git add . && git commit -m "fix(tests): complete test suite synchronization with source code (100% coverage, strict mocks, all files included)"`

**ARCHITECT! INDÍTSD A TASK TREE EGYSZERŰSÍTÉSÉVEL, MAJD HALADJ A LISTÁN FENTRŐL LEFELÉ!**"