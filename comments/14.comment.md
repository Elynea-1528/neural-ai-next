🚀 KIEGÉSZÍTŐ PARANCS 14.(AMENDMENT)

KIEGÉSZÍTÉS A REFAKTORHOZ: EXCEPTION ISOLATION.
A FELADAT: A moduláris felépítés érdekében minden fő komponensnek saját kivétel-osztályokkal kell rendelkeznie. Nem használhatunk általános hibákat.
HOZD LÉTRE A KÖVETKEZŐ FÁJLOKAT (A szabványos exceptions/ mappákban):
1. DATABASE (neural_ai/core/db/exceptions/db_error.py):
Definiáld: DatabaseError (Base), DBConnectionError, TransactionError.
Frissítsd az __init__.py-t exporttal.
1. EVENTS (neural_ai/core/events/exceptions/event_error.py):
Definiáld: EventBusError (Base), PublishError, SubscriberError.
Frissítsd az __init__.py-t.
1. UTILS (neural_ai/core/utils/exceptions/util_error.py):
Definiáld: UtilError (Base), HardwareDetectionError.
Frissítsd az __init__.py-t.
1. REFLEKTÁLÁS A KÓDBAN:
Menj végig a core/db, core/events és core/utils implementációkon.
Cseréld le a sima Exception vagy RuntimeError dobásokat az új, specifikus hibákra!
Példa: A zeromq_bus.py-ban raise RuntimeError("Bus not started") helyett raise EventBusError("Bus not started").
1. ZÁRÁS:
Futtass egy gyors pytest-et (hogy nem törtünk-e el importot).
git add . && git commit -m "feat(core): implement specific exception classes for db, events, and utils"
KEZDD A DB KIVÉTELEK LÉTREHOZÁSÁVAL!"